# Copy this file to .env and fill the values before deploying
# Do NOT commit your real .env to source control.
# Example values:
QUICKNODE_URL="https://your-quicknode-endpoint"
PRIVATE_KEY="0xYOUR_PRIVATE_KEY_HERE"
# Example networks: goerli, sepolia — match the names used in hardhat.config.js
# After creating .env, run the deploy command:
# npx hardhat run scripts/finalDeploy.js --network <network-name>
